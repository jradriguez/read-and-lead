# Native smoke drafts

These flows have not been executed: Maestro and a custom native development build
are not available. They target `com.readandlead.prototype`, never the Expo Go host.
They are not release approval. Verify against the installed Maestro version first.

Use a disposable synthetic-data install. lesson.yaml clears that app's local state;
run resume.yaml afterward to check durable progress. Parent arithmetic/reset behavior
also has UI tests; real accessibility, drag, confirmed reset and offline checks remain
in docs/device-validation.md. There is no hard-coded parent answer in the application.
